function initialize() {
    var mapOptions = {
        //Z
        zoom:15,

        center:new google.maps.LatLng(40.7094, -74.0076),

        mapTypeId: google.maps.MapTypeId.ROADMAP,

        minZoom:2
    }
}